{
    // server configuration and application context variables.
   "server": { 
       "app": {
       }
   },
   // Hapi plugins
   "register": {
        "hapi-pino":{
            "plugin": "require:hapi-pino",
            "options": {
                "ignorePaths": [
                    "/health", 
                    "/private", 
                    "/alive.txt"
                ],
                "logEvents": ["response"]
            }
        }
   }
}